#!/usr/bin/env bash

# Check if TA-Lib should be installed
if grep -q "TA-Lib" requirements.txt 2>/dev/null; then
  echo "Python TA-Lib app detected"
  exit 0
else
  echo "Not a TA-Lib app"
  exit 1
fi
